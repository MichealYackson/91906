import turtle
screen = turtle.Screen()
turtle.colormode(255)
turtle.mode("logo")
Tuples = ()
sigma_turtle = turtle.Turtle()
sigma_turtle.color(0, 0, 0)
screen.register_shape("sigma", Tuples)
sigma_turtle.shape("sigma")
turtle.mainloop()